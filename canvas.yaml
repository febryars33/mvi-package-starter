preset: package

namespace: MVI\Starter
user-auth-model: App\Models\User

paths:
  src: src
  resource: resources

factory:
  path: database/factories

migration:
  path: database/migrations
  prefix: ''

console:
  namespace: MVI\Starter\Console

model:
  namespace: MVI\Starter\Models

provider:
  namespace: MVI\Starter\Providers

testing:
  namespace: MVI\Starter\Tests
